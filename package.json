{
  "name": "mytoolmint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && npm run test:site",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run --passWithNoTests",
    "test:site": "node scripts/check-built-site.mjs",
    "test:watch": "vitest",
    "data:build": "node scripts/build-word-data.mjs"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.13.5",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
